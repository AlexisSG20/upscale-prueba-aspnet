@model SistemaAccesoWeb.Models.LoginViewModel

@{
    ViewData["Title"] = "Gestión de usuarios";
}

<div class="gestion-page">
    <partial name="_Header" />

    <main class="gestion-main">
        <div class="gestion-background">
            <div class="gestion-bg-art"></div>

            <section class="login-panel">
                <div class="document-tabs">
                    <button type="button" class="doc-tab active">DNI</button>
                    <button type="button" class="doc-tab">CE</button>
                </div>

                <form asp-action="GestionUsuarios" asp-controller="Home" method="post" class="login-form" id="loginForm">
                    <div class="form-group">
                        <label asp-for="NumeroDocumento">Usuario</label>
                        <div class="input-icon-wrapper">
                            <img src="~/images/icon-user.svg" alt="Usuario" class="input-icon" />
                            <input asp-for="NumeroDocumento" id="numeroDocumento" placeholder="Ingresar usuario" />
                        </div>
                        <span asp-validation-for="NumeroDocumento" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="Contrasena">Contraseña</label>
                        <div class="input-icon-wrapper password-wrapper">
                            <img src="~/images/icon-lock.svg" alt="Contraseña" class="input-icon" />
                            <input asp-for="Contrasena" id="contrasena" type="password" placeholder="Ingresar contraseña" />
                            <button type="button" class="password-toggle" id="togglePassword" aria-label="Mostrar contraseña">
                                <img src="~/images/icon-eye.svg" alt="Ver contraseña" class="password-eye-icon" />
                            </button>
                        </div>
                        <span asp-validation-for="Contrasena" class="text-danger"></span>
                    </div>

                    @if (TempData["MensajeSesionExpirada"] != null)
                    {
                        <div class="session-expired-toast">
                            <div class="session-expired-toast-icon">i</div>
                            <div class="session-expired-toast-text">
                                @TempData["MensajeSesionExpirada"]
                            </div>
                        </div>
                    }

                    @if (!string.IsNullOrEmpty(Model?.MensajeError))
                    {
                        <div class="text-danger login-error-message">
                            @Model.MensajeError
                        </div>
                    }

                    <a href="#" class="forgot-password">¿Olvidó su contraseña?</a>

                    <button type="submit" class="login-button" id="loginButton">Ingresar</button>
                </form>
            </section>

            <section class="support-box">
                <div class="support-icon">
                    <svg viewBox="0 0 100 100" class="support-icon-img" xmlns="http://www.w3.org/2000/svg" aria-label="Soporte">
                        <path d="M50 15 C75 10 90 30 85 55 C80 85 60 90 40 85 C15 80 10 55 15 35 C20 15 35 18 50 15 Z" fill="#eff4fd" />
                        <path d="M25 25 L29 25 M27 23 L27 27" stroke="#0056b3" stroke-width="2" stroke-linecap="round" />
                        <path d="M80 30 L84 30 M82 28 L82 32" stroke="#0056b3" stroke-width="2" stroke-linecap="round" />

                        <circle cx="50" cy="45" r="14" fill="white" stroke="#0056b3" stroke-width="3" />
                        <path d="M30 80 C30 63 40 60 50 60 C60 60 70 63 70 80 Z" fill="white" stroke="#0056b3" stroke-width="3" stroke-linecap="round" stroke-linejoin="round" />

                        <path d="M32 45 C32 25 68 25 68 45" fill="none" stroke="#0056b3" stroke-width="3" stroke-linecap="round" />
                        <rect x="28" y="40" width="8" height="15" rx="4" fill="#eff4fd" stroke="#0056b3" stroke-width="3" />
                        <rect x="64" y="40" width="8" height="15" rx="4" fill="#eff4fd" stroke="#0056b3" stroke-width="3" />

                        <path d="M68 50 C75 55 60 60 55 60" fill="none" stroke="#0056b3" stroke-width="3" stroke-linecap="round" />
                    </svg>
                </div>
                <p>
                    ¿Necesita ayuda?,
                    <a href="#">contacte con el área de soporte</a>
                </p>
            </section>
        </div>
    </main>

    <partial name="_Footer" />
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const numeroDocumento = document.getElementById("numeroDocumento");
        const contrasena = document.getElementById("contrasena");
        const loginButton = document.getElementById("loginButton");
        const togglePassword = document.getElementById("togglePassword");

        function actualizarEstadoBoton() {
            const usuarioLleno = numeroDocumento.value.trim() !== "";
            const contrasenaLlena = contrasena.value.trim() !== "";

            if (usuarioLleno && contrasenaLlena) {
                loginButton.classList.add("active");
            } else {
                loginButton.classList.remove("active");
            }
        }

        numeroDocumento.addEventListener("input", actualizarEstadoBoton);
        contrasena.addEventListener("input", actualizarEstadoBoton);

        togglePassword.addEventListener("click", function () {
            const esPassword = contrasena.getAttribute("type") === "password";
            contrasena.setAttribute("type", esPassword ? "text" : "password");
        });

        actualizarEstadoBoton();
    });
</script>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const toast = document.querySelector(".session-expired-toast");

        if (!toast) {
            return;
        }

        setTimeout(function () {
            toast.style.opacity = "0";
            toast.style.transform = "translateY(10px)";

            setTimeout(function () {
                toast.remove();
            }, 300);
        }, 5000);
    });
</script>